import styled from 'styled-components';

const Main = styled.main`
  padding: 10rem 0;

  @media screen and (max-width: 720px) {
    padding: 5rem 0;

    h1 {
      font-size: 3.2rem;
    }
  }
`;

export default Main;
