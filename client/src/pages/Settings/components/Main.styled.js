import styled from 'styled-components';

const Main = styled.main`
  padding: 10rem 0;

  input {
    font-size: 1.8rem;
  }

  form {
    button {
      font-size: 2.4rem;
      padding: 2rem 3rem;
    }
  }
`;

export default Main;
